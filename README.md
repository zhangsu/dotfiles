# Su's Dotfiles

Foo.

## Installation

    git clone --recursive git://github.com/zhangsu/dotfiles.git
    cd dotfiles
    rake

## Update submodules

    git submodule update --init --recursive --remote
